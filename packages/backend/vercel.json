{
  "version": 2,
  "installCommand": "npm install --no-package-lock express cors helmet compression dotenv @prisma/client jsonwebtoken winston zod express-rate-limit express-validator bcryptjs nodemailer swagger-jsdoc swagger-ui-express papaparse proj4 csv-parse",
  "buildCommand": "npx prisma generate",
  "outputDirectory": ".",
  "functions": {
    "api/index.js": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ]
}
